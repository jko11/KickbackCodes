
clear all
clear figure

%% Variables 

t_arfi = 0.1875;
filteredData = 1; % 1 for using motion filtered data, 0 for raw data

%% Loading the Files

dataset1 = 
dataset2 = 

load (dataset1)
if filteredData
    arfidata = filtdata;    % Using filtered data
end
focdata = arfidata;
load(dataset2)
if filteredData
    arfidata = filtdata;    % Using filtered data
end
unfocdata = arfidata;



%% Kickback Filter 

[resultantarfi] = kickbackfilter(respath1, respath2, filteredData,t_arfi);

% quickArfiDisplay(arfidata,cc_coef,t,angular,radial, 'Before', t_arfi, 'Before', [0 20], 1, apex);
quickArfiOverlay(arfidata,I,Q,t_bmode_ind,t,angular,radial,iq_axial,ang_string,t_arfi,title_string,arfiLim)